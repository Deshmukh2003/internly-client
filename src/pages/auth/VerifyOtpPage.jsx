import { useState } from "react"; import { Link, useNavigate } from "react-router-dom"; import { toast } from "react-toastify"; import { api } from "../../api/http"; import AuthCard from "../../components/AuthCard";
export default function VerifyOtpPage() { const navigate=useNavigate(); const [email,setEmail]=useState(sessionStorage.getItem("internly-verification-email")||""); const [code,setCode]=useState(""); const [busy,setBusy]=useState(false); const submit=async(e)=>{e.preventDefault();setBusy(true);try{await api("/auth/verify-otp",{method:"POST",body:JSON.stringify({email,code})});toast.success("Email verified. You can now sign in.");navigate("/login");}catch(error){toast.error(error.message);}finally{setBusy(false);}}; const resend=async()=>{try{await api("/auth/resend-otp",{method:"POST",body:JSON.stringify({email})});toast.success("A new code has been sent.");}catch(error){toast.error(error.message);}}; return <AuthCard title="Verify your email" footer={<><span>Wrong email?</span> <Link to="/register">Start again</Link></>}><p className="text-secondary small">Enter the six-digit code sent to your email.</p><form onSubmit={submit}><label className="form-label">Email</label><input className="form-control mb-3" type="email" required value={email} onChange={(e)=>setEmail(e.target.value)}/><label className="form-label">Verification code</label><input className="form-control mb-4" inputMode="numeric" pattern="[0-9]{6}" maxLength="6" required value={code} onChange={(e)=>setCode(e.target.value.replace(/\D/g,""))}/><button className="btn btn-primary w-100" disabled={busy}>{busy?"Verifying…":"Verify email"}</button></form><button className="btn btn-link px-0 mt-3" onClick={resend}>Resend code</button></AuthCard>; }
