import { useState } from "react"; import { toast } from "react-toastify"; import { api } from "../../../api/http";
const empty = { title: "", description: "", companyId: "", domain: "", qualification: "", eligibleBranches: "", location: "", workMode: "", durationWeeks: "", stipend: "", applicationDeadline: "", requiredSkills: "", status: "DRAFT" };
export default function InternshipManagementPanel({ companies, internships, onChanged }) { const [form,setForm]=useState(empty);const[editingId,setEditingId]=useState(null);const[busy,setBusy]=useState(false);const submit=async(e)=>{e.preventDefault();setBusy(true);const payload={...form,companyId:Number(form.companyId),durationWeeks:form.durationWeeks?Number(form.durationWeeks):null,stipend:form.stipend?Number(form.stipend):0,requiredSkills:form.requiredSkills.split(",").map((s)=>s.trim()).filter(Boolean)};try{await api(editingId?`/admin/internships/${editingId}`:"/admin/internships",{method:editingId?"PUT":"POST",body:JSON.stringify(payload)});toast.success(editingId?"Internship updated.":"Internship created.");setForm(empty);setEditingId(null);onChanged();}catch(error){toast.error(error.message);}finally{setBusy(false);}};const edit=(item)=>{setEditingId(item.id);setForm({...item,companyId:item.company.id,requiredSkills:item.requiredSkills.join(", ")});};const deactivate=async(id)=>{if(!window.confirm("Deactivate this internship?"))return;try{await api(`/admin/internships/${id}`,{method:"DELETE"});toast.success("Internship deactivated.");onChanged();}catch(error){toast.error(error.message);}};const update=(key,value)=>setForm({...form,[key]:value});return <div><form className="card border-0 shadow-sm p-4 mb-4" onSubmit={submit}><h2 className="h5">{editingId?"Edit internship":"Create internship"}</h2><div className="row g-3"><Input label="Title" required value={form.title} onChange={(e)=>update("title",e.target.value)}/><div className="col-md-6"><label className="form-label">Company</label><select className="form-select" required value={form.companyId} onChange={(e)=>update("companyId",e.target.value)}><option value="">Choose company</option>{companies.filter((c)=>c.active).map((c)=><option key={c.id} value={c.id}>{c.name}</option>)}</select></div><Input label="Domain" required value={form.domain} onChange={(e)=>update("domain",e.target.value)}/><Input label="Qualification" value={form.qualification||""} onChange={(e)=>update("qualification",e.target.value)}/><Input label="Eligible branches" value={form.eligibleBranches||""} onChange={(e)=>update("eligibleBranches",e.target.value)}/><Input label="Location" value={form.location||""} onChange={(e)=>update("location",e.target.value)}/><Input label="Work mode" placeholder="Remote, Hybrid, On-site" value={form.workMode||""} onChange={(e)=>update("workMode",e.target.value)}/><Input label="Deadline" type="date" required value={form.applicationDeadline||""} onChange={(e)=>update("applicationDeadline",e.target.value)}/><Input label="Duration (weeks)" type="number" min="1" value={form.durationWeeks||""} onChange={(e)=>update("durationWeeks",e.target.value)}/><Input label="Stipend" type="number" min="0" value={form.stipend||""} onChange={(e)=>update("stipend",e.target.value)}/><div className="col-md-6"><label className="form-label">Status</label><select className="form-select" value={form.status} onChange={(e)=>update("status",e.target.value)}><option>DRAFT</option><option>ACTIVE</option><option>CLOSED</option></select></div><div className="col-12"><label className="form-label">Required skills <span className="text-secondary">(comma-separated)</span></label><input className="form-control" required value={form.requiredSkills||""} onChange={(e)=>update("requiredSkills",e.target.value)}/></div><div className="col-12"><label className="form-label">Description</label><textarea className="form-control" rows="4" required value={form.description} onChange={(e)=>update("description",e.target.value)}/></div></div><button className="btn btn-primary mt-3" disabled={busy}>{busy?"Saving…":editingId?"Update internship":"Create internship"}</button>{editingId&&<button type="button" className="btn btn-link mt-3" onClick={()=>{setEditingId(null);setForm(empty);}}>Cancel</button>}</form>{internships.map((item)=><div className="feature-card py-3 mb-2 d-flex justify-content-between gap-3" key={item.id}><div><strong>{item.title}</strong><div className="small text-secondary">{item.company.name} · {item.domain}</div></div><div className="d-flex gap-2 align-items-center"><span className="badge rounded-pill text-bg-light border">{item.status}</span><button className="btn btn-sm btn-outline-primary" onClick={()=>edit(item)}>Edit</button>{item.status!=="INACTIVE"&&<button className="btn btn-sm btn-outline-danger" onClick={()=>deactivate(item.id)}>Deactivate</button>}</div></div>)}</div>}
function Input({label,wide,...props}){return <div className={wide?"col-12":"col-md-6"}><label className="form-label">{label}</label><input className="form-control" {...props}/></div>}
