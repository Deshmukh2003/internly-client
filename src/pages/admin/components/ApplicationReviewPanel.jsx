import { toast } from "react-toastify"; import { api } from "../../../api/http";
export default function ApplicationReviewPanel({ applications, onChanged }) { const changeStatus=async(id,status)=>{try{await api(`/admin/applications/${id}/status`,{method:"PATCH",body:JSON.stringify({status})});toast.success("Application status updated.");onChanged();}catch(error){toast.error(error.message);}};return applications.length?applications.map((item)=><div className="feature-card py-3 mb-2 d-flex flex-wrap justify-content-between align-items-center gap-3" key={item.id}><div><strong>{item.internship.title}</strong><div className="small text-secondary">{item.studentEmail} · {item.status}</div></div><select className="form-select form-select-sm status-select" value={item.status} onChange={(e)=>changeStatus(item.id,e.target.value)}><option>SUBMITTED</option><option>SHORTLISTED</option><option>ACCEPTED</option><option>REJECTED</option></select></div>):<div className="feature-card">No applications yet.</div>;}
